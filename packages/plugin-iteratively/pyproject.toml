[tool.poetry]
name = "itly-plugin-iteratively"
version = "0.1.14"
description = "Iteratively Analytics SDK - Iteratively Plugin"
readme="README.md"
authors = ["Iteratively"]
license = "MIT"
packages = [
    { include = "itly_plugin_iteratively" }
]

[tool.poetry.dependencies]
python = "^3.6"
requests = "^2.24.0"

[tool.poetry.dev-dependencies]
pytest = "^6.0.1"
itly-sdk = { path="../sdk", develop=true }

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
